package patterns.longestInterval;

/**
 * below solution can be used for questions like:
 *      1- given an array, find the interval whose sum >= k
 *      2- given an array containing hours worked by an employee per day,
 *         if hours-worked > 8 => good day else normal day
 *         find the interval for which good days > normal days
 */
public class FindLongestInterval {
}
